import styles from "./modal.module.css";

export default function Modal() {
  return(
    <div>
      <div>
        <input type="text" />
        <button>Cancel</button>
        <button>Save</button>
      </div>
    </div>
  )
}