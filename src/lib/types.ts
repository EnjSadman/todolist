export enum fetchMethods {
  get = "GET",
  post = "POST",
  put = "PUT",
  delete = "DELETE",
}

export enum fetchType {
  posts = "posts",
  comments = "comments",
  albums = "albums",
  photos = "photos",
  todos = "todos",
  users = "users",
}